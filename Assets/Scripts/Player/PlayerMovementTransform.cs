using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class PlayerMovementTransform : MonoBehaviour
{
/*    [SerializeField]
    private float _speed = 10f;
    [SerializeField]
    private float _acceleration = 10f;

    private CharacterAnimator _characterAnimator;

    private InputAction _moveAction;

    private void Start()
    {
        _characterAnimator = GetComponent<CharacterAnimator>();
        _moveAction = S.I.IM.PC.Gameplay.Move;
    }

    private void Update()
    {
        float movement = _moveAction.ReadValue<float>();

        // Set animation parameters. 
        _characterAnimator.MoveX = movement;
        if (Mathf.Abs(movement) > 0.5f)
        {
            _characterAnimator.LastX = movement;
        }

        float desiredSpeed = movement * _speed;


    }*/
}